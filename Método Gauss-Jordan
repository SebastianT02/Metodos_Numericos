%Método de Gauss-Jordan
%En el siguiente programa se puede determinar la solución de una matriz mxn.
%La idea de este programa es ir realizando el procedimiento paso a paso para  finalmente llegar a la respuesta 
%Ejemplo: A = [1,-1,0,3; 2,1,-1,1; 3,-1,-1,2; -1,2,3,-1] y B = [-1;0;2;4]
%Para llegar a la respuesta en el ejemplo recorre el codigo elimiando los numeros debajo de los pivotes, es decir; haciendo ceros

clear,clc %Limpia la ventana de comandos

A = [1,-1,0,3; 2,1,-1,1; 3,-1,-1,2; -1,2,3,-1];
b = [-1;0;2;4];
fprintf('Matriz actual\n')
Ab=[A b]; %Datos de la matriz

for i=1:length(Ab(:,1)) %para i desde la primera fila  hasta el número de filas existentes 
if Ab(i,i)~=1 %si el elemento i,i de la diagonal es diferente de 1 
    Ab(i,:)= Ab(i,:)./Ab(i,i);  %entonces se convierte a 1 dividiendo toda la fila por dicho elemento
    
end
Ab %muestra la matriz
%Además el resto de elementos de la columna deben convertirse a 0 :  
%es decir si  n es diferente de i ya que si i y n son iguales entonces el elemento se encuentra en la diagonal
    for n=1:length(Ab(:,1)) %Para n desde la primera fila hasta el número de filas existentes
      if n~=i % si n en la columna i no está en la diagonal es decir si i no es igual a n
          Ab(n,:)=-Ab(n,i).*Ab(i,:)+Ab(n,:); %entonces se convierte en 0  
      end 
    end  
end
fprintf('Matriz reducida\n')
Ab %muestra la matriz
